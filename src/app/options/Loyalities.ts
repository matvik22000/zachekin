export class Loyalities {
  code: number;
  number: number;
  disabled: boolean;
  constructor(code: number, number: number, disabled: boolean) {
    this.code = code;
    this.number = number;
    this.disabled = disabled
  }
}
